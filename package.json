{
  "name": "@taqlyn/nav-expo-router",
  "version": "0.1.0",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "description": "Optional Expo Router helpers for Taqlyn deferred links (ready-gate bootstrap only — no Match logic)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "taqlyn",
    "expo",
    "expo-router",
    "deep-links",
    "deferred-deep-links"
  ],
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@taqlyn/sdk-contract": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.2"
  }
}
